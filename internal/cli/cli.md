# CLI

Collection of all CLI Commands that are supported. We treat the CLI commands like UI and not a place for logic.
If logic gets into the CLI commands we need to refactor them into a service or some business arch pattern. 

We're shooting for Domain Models and supporting cast of patterns to help it.